const d: Date = new Date();
console.log(d.getTime()); //1630522759764, the getTime() method returns the number of milliseconds between midnight of January 1, 1970 and the specified date, a.k.a, the Unix epoch (or Unix time or POSIX time or Unix timestamp)
